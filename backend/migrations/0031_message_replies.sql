-- ═══════════════════════════════════════════════════════════════════════════
-- 0031_message_replies.sql — «ریپلای» در چت (بخش ۱۰ سند):
-- هر پیام می‌تواند به یک پیام قبلیِ همان گفتگو پاسخ بدهد. کلاینت پیش‌نمایش
-- پیامِ نقل‌شده را از روی همین شناسه (که در فهرست همان گفتگو موجود است)
-- نمایش می‌دهد — در همهٔ سطح‌ها یکسان: چت هم‌صنفی‌ها، ارتباط با مدیریت
-- (شاگرد/والد/استاد) و پاسخ مدیر از داشبورد نظارتی.
--
-- اجرا:
--   wrangler d1 execute afghan_girls_school_db --remote --file=./migrations/0031_message_replies.sql
-- ═══════════════════════════════════════════════════════════════════════════

ALTER TABLE messages ADD COLUMN reply_to_id TEXT;
