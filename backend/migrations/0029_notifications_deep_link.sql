-- ═══════════════════════════════════════════════════════════════════════════
-- 0029_notifications_deep_link.sql — رفع اشکال: تا اینجا اعلان‌ها هیچ اشاره‌ای
-- به «موضوع واقعیِ» خودشان نداشتند (کدام امتحان، کدام گفتگو، کدام سمینار...)
-- یعنی لمس یک اعلان در برنامه فقط آن را «خوانده‌شده» می‌کرد و هیچ‌جا باز
-- نمی‌شد. این ستون شناسهٔ رکورد مرتبط را نگه می‌دارد تا کلاینت بتواند مستقیم
-- به همان صفحه هدایت کند (بخش‌های ۱۳.۱ سند).
--
-- همچنین `kind` را از حالت تک‌مقدارِ سرگردانِ 'general' برای هم «کار خانگی»
-- و هم «درخواست پیوند والد»/«ثبت‌نام تازه» دقیق‌تر می‌کنیم — چون این دو نوع
-- رویداد، مقصدهای متفاوتی در اپ دارند (کار خانگی → صفحهٔ کار خانگی،
-- درخواست پیوند/ثبت‌نام → پروفایل/مدیریت کاربران) و با یک kind مشترک قابل
-- تشخیص از هم نبودند.
--
-- اجرا:
--   wrangler d1 execute afghan_girls_school_db --remote --file=./migrations/0029_notifications_deep_link.sql
-- ═══════════════════════════════════════════════════════════════════════════

ALTER TABLE notifications ADD COLUMN related_id TEXT;
