-- ═══════════════════════════════════════════════════════════════════════════
-- 0025_academy_books_pdf.sql — رفع اشکال «کتابخانه»: تا اکنون آپلود/دانلود
-- پی‌دی‌افِ کتاب‌های آکادمی (academy_books) کاملاً شبیه‌سازی‌شده بود (نه فایل
-- واقعی روی سرور، نه دانلود واقعی توسط شاگرد). این ستون کلید فایل را در R2
-- (همان Bucket که برای عکس پروفایل/فایل‌های صوتی هم استفاده می‌شود) نگه
-- می‌دارد تا از طریق endpoint عمومی `GET /files/*` قابل بازیابی باشد.
-- اجرا:
--   npx wrangler d1 execute afghan_girls_school_db --remote --file=./migrations/0025_academy_books_pdf.sql
-- ═══════════════════════════════════════════════════════════════════════════

ALTER TABLE academy_books ADD COLUMN pdf_key TEXT NOT NULL DEFAULT '';
