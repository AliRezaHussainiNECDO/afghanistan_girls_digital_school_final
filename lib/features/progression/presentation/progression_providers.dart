/// انبار ارتقای صنف — تعریف اصلی به `core/student/selected_grade_provider.dart`
/// منتقل شد (به‌صورت ChangeNotifierProvider، تا ارتقا/تغییر پیشرفت خودکار به
/// همهٔ صفحه‌ها منتشر شود). این فایل برای سازگاری، همان را re-export می‌کند.
export '../../../core/student/selected_grade_provider.dart'
    show progressionStoreProvider, activeGradeProvider;
