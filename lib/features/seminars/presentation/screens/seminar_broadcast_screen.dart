// این فایل عمداً خالی شده است — رفعِ ریشه‌ایِ اشکال «استاد هنگام شروع
// سمینار از برنامه خارج می‌شود» (خصوصاً روی گوشی‌های سامسونگ).
//
// کلاسِ قبلیِ `SeminarBroadcastScreen` مسیر «پخش مستقیم دوربین از همین اپ»
// را با پکیج `apivideo_live_stream` پیاده می‌کرد. تحقیق روی ردیاب مشکلاتِ
// همان پکیج نشان داد لحظهٔ اتصال RTMPS باعث یک Crash بومی (SIGSEGV در
// libssl) روی برخی دستگاه‌ها/نسخه‌های اندروید می‌شود — مشکلی که خودِ
// سازندهٔ پکیج هم تأیید کرده در سطح کتابخانه رفع‌شدنی نیست. چون Crash در
// کد بومی رخ می‌دهد، هیچ try/catch یا `runZonedGuarded` سمت Dart نمی‌تواند
// جلویش را بگیرد؛ تنها راهِ واقعیِ حذفِ ریشهٔ مشکل این بود که این مسیر و
// وابستگی‌اش کاملاً از پروژه حذف شوند (نه فقط مخفی/دمُوت‌شده).
//
// اکنون همهٔ جریان‌های «شروع/ورود به سمینار» تنها از دو مسیرِ پایدار
// استفاده می‌کنند (`go_live_flow.dart`):
//   ۱) پخش با نرم‌افزار خارجی (OBS/Larix) روی Cloudflare Stream،
//   ۲) اتاق ویدیوکنفرانس داخلی با Jitsi Meet SDK (`seminar_room_screen.dart`).
//
// وابستگیِ `apivideo_live_stream` هم از `pubspec.yaml` حذف شده است.
