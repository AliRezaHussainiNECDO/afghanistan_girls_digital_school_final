# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Android signing (never commit real keystores/credentials)
/android/key.properties
*.jks
*.keystore

# Backend (Cloudflare Worker) — secrets and local dev artifacts
backend/node_modules/
backend/.dev.vars
backend/.wrangler/
backend/*.tmp

# فایل‌های موقتِ لاگِ adb logcat (بازتولید کرشِ سمینار روی گوشی واقعی) —
# حجیم (تا ۹۰+ مگابایت) و صرفاً برای دیباگ محلی بودند، نباید وارد گیت شوند.
crash.txt*
full.txt
live.txt
live_utf8.txt
result.txt
result2.txt
